import GalleryClient from './GalleryClient';

export const metadata = {
  title: "Gallery | Dazzle Cleaning",
  description: "View our full gallery of spotless results from homes and commercial spaces across Melbourne.",
};

export default function GalleryPage() {
    return <GalleryClient />;
}
